
### function startWheelSpin
ap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled

### startWheelSpin(
alBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad

### startWheelSpin(
.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(ac

### startWheelSpin(
nt.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled':'')+'>🎬

### fortuneAds
л Эхо',6000,'Разрешает установку компонентов в Проект Эхо.','triple',8]];const levelReq=[0,100,250,450,700,1000,1400,1900,2500,3200];const sellPrices={sample:0.1,blood:2,dna:6.67,neural:16.67,bone:28.33,reagent:6,crystal:50,virus:73.33,bioenergy:86.67,serum:60,gel:73.33,regen:80,matrix:166.67,mdna:200,heart:300,adaptedHeart:500,stableHeart:833.33,echoCore:1666.67};const buyPrices={sample:8,blood:24,dna:70,neural:140,bone:170,reagent:36,crystal:300,virus:440,bioenergy:520,serum:360,gel:440,regen:null,matrix:null,mdna:null,heart:1800,adaptedHeart:3000,stableHeart:null,echoCore:null};const base={version:SAVE_SCHEMA_VERSION,introSeen:false,money:120,research:60,energy:100,capacity:30,labLevel:1,prestige:0,lastSave:0,lastOffline:nowMs(),exp:{},hinted:[],discovered:['sample','blood','reagent'],installed:{},echoDone:false,meta:{bestLevel:1,totalExperiments:0,totalSales:0,totalOrders:0,resets:0,tutorialStartedAt:0,eventsSeen:0},storySeen:{},orders:{day:'',active:[],claimed:{}},event:{id:'',expires:0},machines:{incubator:1,culture:0,analyzer:0,tissue:0,regenerator:0,synth:0,crystal:0,virusLab:0},tech:{precision:0,analysis:0,adaptation:0,safety:0,triple:0,echo:0},fortuneDay:'',fortuneFree:3,fortuneAds:0,tasks:{experiment:0,sell:0,produce:0,claimed:{},active:['experiment','sell','produce'],refreshDay:'',refreshFree:3,refreshAds:0},adDay:'',adCounts:{warehouse:0,discount:0,production:0,money:0,research:0,energy:0},adWarehouseUntil:0,adDiscountUntil:0,adProductionUntil:0};for(const k of keys)base[k]=0;base.sample=8;base.blood=5;base.reagent=3;let s=clone(base);function clone(x){return JSON.parse(JSON.stringify(x))}function num(x){return Number.isFinite(Number(x))?Number(x):0}function fmt(x){x=Math.floor(num(x));return x<1000?String(x):x<1000000?(x/1000).toFixed(1)+'K':(x/1000000).toFixed(1)+'M'}function toast(t){let e=document.getElementById('toast');if(!e){e=document.createElement('div');e.id='toast';e.className='toast';document.body.appendChild(e)}e.textContent=t;e.classList.remove('hidden');e.style.cssText='position:fixed;left:50%;bottom:100px;transform:translateX(-50%);z-index:99999;max-width:min(420px,92vw);padding:12px 16px;border-radius:14px;background:rgba(8,18,30,.96);border:1px solid rgba(94,234,212,.45);color:#e8f4f8;font-weight:700;font-size:14px;box-shadow:0 12px 40px rgba(0,0,0,.45);display:block;pointer-events:none';clearTimeout(toast.timer);toast.timer=setTimeout(()=>{e.classList.add('hidden');e.style.display='none'},2800)}function combo(a){return [...a].sort().join('+')}function reaction(a){return reactions.find(r=>combo(r.a)===combo(a))}function icon(k){return items[k]?.[0]||'🔬'}function label(k){return items[k]?.[1]||k}function discover(k){if(!s.discovered.includes(k)){s.discovered.push(k);toast('📖 Открыто: '+label(k));showStoryNote(k)}}function showStoryNote(k,force=false){const notes={serum:['Запись №017 · Генетическая сыворотка','Первый стабильный результат.','<p>«Клетки приняли изменённую последовательность без отторжения. Я наконец вижу путь к управляемой перестройке человеческой ткани.</p><div class="story-quote">Ассистент снова просит прекратить опыты. Он слишком привязан к старым представлениям о том, что человек не должен становиться другим.</div>'],gel:['Запись №024 · Нейрогель','Профессор перешёл к экспериментам с нервной системой.','<p>«Нейронные связи можно не просто восстанавливать — их можно переписывать. Подопытный №12 начал вспоминать то, чего с ним никогда не происходило.»</p>'],regen:['Запись №031 · Регенеративная культура','Эксперимент впервые пережил тяжёлое повреждение.','<p>«Ткань восстанавливается практически без рубцевания. Я перенёс образец в нижнюю камеру. Там безопаснее.»</p><p>Странно, но после этого ассистент перестал спускаться в подвал.</p>'],mdna:['Запись №043 · Модифицированная ДНК','Теперь профессор смешивает несовместимое.','<p>«Человеческая ДНК и вирусный вектор дали результат. Изменения закрепляются. Следующий шаг — добавить свойства другого вида.»</p><div class="story-quote">Ассистент назвал это созданием чудовища. Я назвал это адаптацией.</div>'],matrix:['Запись №052 · Нейроматрица','Граница между человеком и созданным организмом стирается.','<p>«Матрица сохраняет память, даже 

### fortuneAds
=1;if(x.exp&&typeof x.exp==='object')for(const r of reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast

### fortuneAds
 reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast('Открой склад и выбери материал');try{openLabModal

### fortuneAds
p[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast('Открой склад и выбери материал');try{openLabModal('warehouse')}catch(e){}return}modal.dataset.slot=String(slot);modal.classList.re

### fortuneAds
;
e.innerHTML=html;
e.querySelectorAll('[data-market-action]').forEach(b=>b.onclick=()=>{try{marketAction(b.dataset.marketAction)}catch(err){console.error(err);toast('Ошибка сделки')}});
const ad=e.querySelector('[data-market-ad="discount"]');if(ad)ad.onclick=()=>claimAdBonus('discount');
document.querySelectorAll('[data-market-tab]').forEach(x=>{if(x&&x.dataset)x.classList.toggle('active',x.dataset.marketTab===mode)})}catch(err){console.error('renderMarket',err);const e=document.getElementById('marketList');if(e)e.innerHTML='<div class="notice">Рынок загружается…</div><button type="button" class="primary" id="marketRetryBtn">Показать товары</button>';const r=document.getElementById('marketRetryBtn');if(r)r.onclick=()=>renderMarket()}}
function wheelPool(){return[['sample',42.7],['blood',20.7],['reagent',12.6],['dna',7.2],['bioenergy',3.6],['serum',2.25],['crystal',.72],['gel',.135],['matrix',.09],['stableHeart',.005],['nothing',10]]}
function weightedWheel(){const pool=wheelPool();let total=pool.reduce((a,x)=>a+x[1],0),r=Math.random()*total;for(const x of pool){r-=x[1];if(r<=0)return x[0]}return'nothing'}
let wheelAngle=0;
function renderFortune(){const free=s.fortuneFree|0,ads=s.fortuneAds|0;let status='';if(free>0)status='<div class="wheel-status">Бесплатно: <b>'+free+'/3</b></div>';else if(ads<10)status='<div class="wheel-status wheel-status-ad">🎬 Реклама · осталось '+(10-ads)+'</div>';else status='<div class="wheel-status">Лимит на сегодня</div>';const html='<div class="wheel-wrap" id="wheelTap" role="button" tabindex="0"><div class="wheel-pointer">▼</div><div class="wheel" id="wheelVisual" style="transform:rotate('+wheelAngle+'deg)">🎡</div></div><div class="wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи на

### fortuneAds
ap" role="button" tabindex="0"><div class="wheel-pointer">▼</div><div class="wheel" id="wheelVisual" style="transform:rotate('+wheelAngle+'deg)">🎡</div></div><div class="wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных доброво

### fortuneAds
s="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.a

### fortuneAds
font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · акти

### fortuneFree
','⚡','Протокол Эхо',6000,'Разрешает установку компонентов в Проект Эхо.','triple',8]];const levelReq=[0,100,250,450,700,1000,1400,1900,2500,3200];const sellPrices={sample:0.1,blood:2,dna:6.67,neural:16.67,bone:28.33,reagent:6,crystal:50,virus:73.33,bioenergy:86.67,serum:60,gel:73.33,regen:80,matrix:166.67,mdna:200,heart:300,adaptedHeart:500,stableHeart:833.33,echoCore:1666.67};const buyPrices={sample:8,blood:24,dna:70,neural:140,bone:170,reagent:36,crystal:300,virus:440,bioenergy:520,serum:360,gel:440,regen:null,matrix:null,mdna:null,heart:1800,adaptedHeart:3000,stableHeart:null,echoCore:null};const base={version:SAVE_SCHEMA_VERSION,introSeen:false,money:120,research:60,energy:100,capacity:30,labLevel:1,prestige:0,lastSave:0,lastOffline:nowMs(),exp:{},hinted:[],discovered:['sample','blood','reagent'],installed:{},echoDone:false,meta:{bestLevel:1,totalExperiments:0,totalSales:0,totalOrders:0,resets:0,tutorialStartedAt:0,eventsSeen:0},storySeen:{},orders:{day:'',active:[],claimed:{}},event:{id:'',expires:0},machines:{incubator:1,culture:0,analyzer:0,tissue:0,regenerator:0,synth:0,crystal:0,virusLab:0},tech:{precision:0,analysis:0,adaptation:0,safety:0,triple:0,echo:0},fortuneDay:'',fortuneFree:3,fortuneAds:0,tasks:{experiment:0,sell:0,produce:0,claimed:{},active:['experiment','sell','produce'],refreshDay:'',refreshFree:3,refreshAds:0},adDay:'',adCounts:{warehouse:0,discount:0,production:0,money:0,research:0,energy:0},adWarehouseUntil:0,adDiscountUntil:0,adProductionUntil:0};for(const k of keys)base[k]=0;base.sample=8;base.blood=5;base.reagent=3;let s=clone(base);function clone(x){return JSON.parse(JSON.stringify(x))}function num(x){return Number.isFinite(Number(x))?Number(x):0}function fmt(x){x=Math.floor(num(x));return x<1000?String(x):x<1000000?(x/1000).toFixed(1)+'K':(x/1000000).toFixed(1)+'M'}function toast(t){let e=document.getElementById('toast');if(!e){e=document.createElement('div');e.id='toast';e.className='toast';document.body.appendChild(e)}e.textContent=t;e.classList.remove('hidden');e.style.cssText='position:fixed;left:50%;bottom:100px;transform:translateX(-50%);z-index:99999;max-width:min(420px,92vw);padding:12px 16px;border-radius:14px;background:rgba(8,18,30,.96);border:1px solid rgba(94,234,212,.45);color:#e8f4f8;font-weight:700;font-size:14px;box-shadow:0 12px 40px rgba(0,0,0,.45);display:block;pointer-events:none';clearTimeout(toast.timer);toast.timer=setTimeout(()=>{e.classList.add('hidden');e.style.display='none'},2800)}function combo(a){return [...a].sort().join('+')}function reaction(a){return reactions.find(r=>combo(r.a)===combo(a))}function icon(k){return items[k]?.[0]||'🔬'}function label(k){return items[k]?.[1]||k}function discover(k){if(!s.discovered.includes(k)){s.discovered.push(k);toast('📖 Открыто: '+label(k));showStoryNote(k)}}function showStoryNote(k,force=false){const notes={serum:['Запись №017 · Генетическая сыворотка','Первый стабильный результат.','<p>«Клетки приняли изменённую последовательность без отторжения. Я наконец вижу путь к управляемой перестройке человеческой ткани.</p><div class="story-quote">Ассистент снова просит прекратить опыты. Он слишком привязан к старым представлениям о том, что человек не должен становиться другим.</div>'],gel:['Запись №024 · Нейрогель','Профессор перешёл к экспериментам с нервной системой.','<p>«Нейронные связи можно не просто восстанавливать — их можно переписывать. Подопытный №12 начал вспоминать то, чего с ним никогда не происходило.»</p>'],regen:['Запись №031 · Регенеративная культура','Эксперимент впервые пережил тяжёлое повреждение.','<p>«Ткань восстанавливается практически без рубцевания. Я перенёс образец в нижнюю камеру. Там безопаснее.»</p><p>Странно, но после этого ассистент перестал спускаться в подвал.</p>'],mdna:['Запись №043 · Модифицированная ДНК','Теперь профессор смешивает несовместимое.','<p>«Человеческая ДНК и вирусный вектор дали результат. Изменения закрепляются. Следующий шаг — добавить свойства другого вида.»</p><div class="story-quote">Ассистент назвал это созданием чудовища. Я назвал это адаптацией.</div>'],matrix:['Запись №052 · Нейроматрица','Граница между человеком и созданным организмом стирается.','<p>«Матрица сохраняет

### fortuneFree
for(const r of reactions)if(x.installed[r.out]===1)z.installed[r.out]=1;if(x.exp&&typeof x.exp==='object')for(const r of reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){co

### fortuneFree
z.installed[r.out]=1;if(x.exp&&typeof x.exp==='object')for(const r of reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker'

### fortuneFree
h.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast('Открой склад и выбери материал');try{openLabModal('warehouse')}catch(e){}return}modal.dataset.slot=String(slot);mo

### fortuneFree
eturn ''}}).join('');
e.innerHTML=html;
e.querySelectorAll('[data-market-action]').forEach(b=>b.onclick=()=>{try{marketAction(b.dataset.marketAction)}catch(err){console.error(err);toast('Ошибка сделки')}});
const ad=e.querySelector('[data-market-ad="discount"]');if(ad)ad.onclick=()=>claimAdBonus('discount');
document.querySelectorAll('[data-market-tab]').forEach(x=>{if(x&&x.dataset)x.classList.toggle('active',x.dataset.marketTab===mode)})}catch(err){console.error('renderMarket',err);const e=document.getElementById('marketList');if(e)e.innerHTML='<div class="notice">Рынок загружается…</div><button type="button" class="primary" id="marketRetryBtn">Показать товары</button>';const r=document.getElementById('marketRetryBtn');if(r)r.onclick=()=>renderMarket()}}
function wheelPool(){return[['sample',42.7],['blood',20.7],['reagent',12.6],['dna',7.2],['bioenergy',3.6],['serum',2.25],['crystal',.72],['gel',.135],['matrix',.09],['stableHeart',.005],['nothing',10]]}
function weightedWheel(){const pool=wheelPool();let total=pool.reduce((a,x)=>a+x[1],0),r=Math.random()*total;for(const x of pool){r-=x[1];if(r<=0)return x[0]}return'nothing'}
let wheelAngle=0;
function renderFortune(){const free=s.fortuneFree|0,ads=s.fortuneAds|0;let status='';if(free>0)status='<div class="wheel-status">Бесплатно: <b>'+free+'/3</b></div>';else if(ads<10)status='<div class="wheel-status wheel-status-ad">🎬 Реклама · осталось '+(10-ads)+'</div>';else status='<div class="wheel-status">Лимит на сегодня</div>';const html='<div class="wheel-wrap" id="wheelTap" role="button" tabindex="0"><div class="wheel-pointer">▼</div><div class="wheel" id="wheelVisual" style="transform:rotate('+wheelAngle+'deg)">🎡</div></div><div class="wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toa

### fortuneFree
;const html='<div class="wheel-wrap" id="wheelTap" role="button" tabindex="0"><div class="wheel-pointer">▼</div><div class="wheel" id="wheelVisual" style="transform:rotate('+wheelAngle+'deg)">🎡</div></div><div class="wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Р

### fortuneFree
"wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActiv

### fortuneFree
=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled':'')+'>🎬 Получить</button></div>'}).join('')+'</div>';e.querySelectorAll('[data-ad-bonus]'

### fortuneDay
ety',7],['echo','⚡','Протокол Эхо',6000,'Разрешает установку компонентов в Проект Эхо.','triple',8]];const levelReq=[0,100,250,450,700,1000,1400,1900,2500,3200];const sellPrices={sample:0.1,blood:2,dna:6.67,neural:16.67,bone:28.33,reagent:6,crystal:50,virus:73.33,bioenergy:86.67,serum:60,gel:73.33,regen:80,matrix:166.67,mdna:200,heart:300,adaptedHeart:500,stableHeart:833.33,echoCore:1666.67};const buyPrices={sample:8,blood:24,dna:70,neural:140,bone:170,reagent:36,crystal:300,virus:440,bioenergy:520,serum:360,gel:440,regen:null,matrix:null,mdna:null,heart:1800,adaptedHeart:3000,stableHeart:null,echoCore:null};const base={version:SAVE_SCHEMA_VERSION,introSeen:false,money:120,research:60,energy:100,capacity:30,labLevel:1,prestige:0,lastSave:0,lastOffline:nowMs(),exp:{},hinted:[],discovered:['sample','blood','reagent'],installed:{},echoDone:false,meta:{bestLevel:1,totalExperiments:0,totalSales:0,totalOrders:0,resets:0,tutorialStartedAt:0,eventsSeen:0},storySeen:{},orders:{day:'',active:[],claimed:{}},event:{id:'',expires:0},machines:{incubator:1,culture:0,analyzer:0,tissue:0,regenerator:0,synth:0,crystal:0,virusLab:0},tech:{precision:0,analysis:0,adaptation:0,safety:0,triple:0,echo:0},fortuneDay:'',fortuneFree:3,fortuneAds:0,tasks:{experiment:0,sell:0,produce:0,claimed:{},active:['experiment','sell','produce'],refreshDay:'',refreshFree:3,refreshAds:0},adDay:'',adCounts:{warehouse:0,discount:0,production:0,money:0,research:0,energy:0},adWarehouseUntil:0,adDiscountUntil:0,adProductionUntil:0};for(const k of keys)base[k]=0;base.sample=8;base.blood=5;base.reagent=3;let s=clone(base);function clone(x){return JSON.parse(JSON.stringify(x))}function num(x){return Number.isFinite(Number(x))?Number(x):0}function fmt(x){x=Math.floor(num(x));return x<1000?String(x):x<1000000?(x/1000).toFixed(1)+'K':(x/1000000).toFixed(1)+'M'}function toast(t){let e=document.getElementById('toast');if(!e){e=document.createElement('div');e.id='toast';e.className='toast';document.body.appendChild(e)}e.textContent=t;e.classList.remove('hidden');e.style.cssText='position:fixed;left:50%;bottom:100px;transform:translateX(-50%);z-index:99999;max-width:min(420px,92vw);padding:12px 16px;border-radius:14px;background:rgba(8,18,30,.96);border:1px solid rgba(94,234,212,.45);color:#e8f4f8;font-weight:700;font-size:14px;box-shadow:0 12px 40px rgba(0,0,0,.45);display:block;pointer-events:none';clearTimeout(toast.timer);toast.timer=setTimeout(()=>{e.classList.add('hidden');e.style.display='none'},2800)}function combo(a){return [...a].sort().join('+')}function reaction(a){return reactions.find(r=>combo(r.a)===combo(a))}function icon(k){return items[k]?.[0]||'🔬'}function label(k){return items[k]?.[1]||k}function discover(k){if(!s.discovered.includes(k)){s.discovered.push(k);toast('📖 Открыто: '+label(k));showStoryNote(k)}}function showStoryNote(k,force=false){const notes={serum:['Запись №017 · Генетическая сыворотка','Первый стабильный результат.','<p>«Клетки приняли изменённую последовательность без отторжения. Я наконец вижу путь к управляемой перестройке человеческой ткани.</p><div class="story-quote">Ассистент снова просит прекратить опыты. Он слишком привязан к старым представлениям о том, что человек не должен становиться другим.</div>'],gel:['Запись №024 · Нейрогель','Профессор перешёл к экспериментам с нервной системой.','<p>«Нейронные связи можно не просто восстанавливать — их можно переписывать. Подопытный №12 начал вспоминать то, чего с ним никогда не происходило.»</p>'],regen:['Запись №031 · Регенеративная культура','Эксперимент впервые пережил тяжёлое повреждение.','<p>«Ткань восстанавливается практически без рубцевания. Я перенёс образец в нижнюю камеру. Там безопаснее.»</p><p>Странно, но после этого ассистент перестал спускаться в подвал.</p>'],mdna:['Запись №043 · Модифицированная ДНК','Теперь профессор смешивает несовместимое.','<p>«Человеческая ДНК и вирусный вектор дали результат. Изменения закрепляются. Следующий шаг — добавить свойства другого вида.»</p><div class="story-quote">Ассистент назвал это созданием чудовища. Я назвал это адаптацией.</div>'],matrix:['Запись №052 · Нейроматрица','Граница между человеком и созданным организмом стирается.','<p>«Мат

### fortuneDay
typeof x.installed==='object')for(const r of reactions)if(x.installed[r.out]===1)z.installed[r.out]=1;if(x.exp&&typeof x.exp==='object')for(const r of reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}functio

### fortuneDay
alled==='object')for(const r of reactions)if(x.installed[r.out]===1)z.installed[r.out]=1;if(x.exp&&typeof x.exp==='object')for(const r of reactions){const id=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openCompone

### fortuneDay
=combo(r.a);if(num(x.exp[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast('Открой склад и выбери материал');try{openLabModal('warehouse')}catch(

### fortuneDay
[id])>0)z.exp[id]=Math.floor(num(x.exp[id]))}if(Array.isArray(x.hinted))z.hinted=[...new Set(x.hinted.filter(v=>reactions.some(r=>combo(r.a)===v)))];if(Array.isArray(x.discovered))z.discovered=[...new Set(x.discovered.filter(v=>keys.includes(v)))];if(x.tasks&&typeof x.tasks==='object'){z.tasks.experiment=Math.max(0,num(x.tasks.experiment));z.tasks.sell=Math.max(0,num(x.tasks.sell));z.tasks.produce=Math.max(0,num(x.tasks.produce));z.tasks.claimed=x.tasks.claimed&&typeof x.tasks.claimed==='object'?x.tasks.claimed:{};z.tasks.active=Array.isArray(x.tasks.active)?x.tasks.active.filter(v=>typeof v==='string'&&v):['exp5','sell20','prod100'];z.tasks.refreshDay=x.tasks.refreshDay||'';z.tasks.refreshFree=Math.max(0,Math.min(3,Math.floor(num(x.tasks.refreshFree))));z.tasks.refreshAds=Math.max(0,Math.min(5,Math.floor(num(x.tasks.refreshAds))))}if(x.adCounts&&typeof x.adCounts==='object')for(const k of Object.keys(z.adCounts))z.adCounts[k]=Math.max(0,Math.floor(num(x.adCounts[k])));z.adDay=x.adDay||'';z.fortuneDay=x.fortuneDay||'';z.fortuneFree=Math.max(0,Math.min(3,Math.floor(num(x.fortuneFree))));z.fortuneAds=Math.max(0,Math.min(10,Math.floor(num(x.fortuneAds))));if(z.fortuneDay!==today()){z.fortuneDay=today();z.fortuneFree=3;z.fortuneAds=0}if(z.adDay!==today()){z.adDay=today();z.adCounts=clone(base.adCounts)}if(z.tasks.refreshDay!==today()){z.tasks.refreshDay=today();z.tasks.refreshFree=3;z.tasks.refreshAds=0;if(!Array.isArray(z.tasks.active)||z.tasks.active.length!==3)z.tasks.active=['experiment','sell','produce'];z.tasks.experiment=0;z.tasks.sell=0;z.tasks.produce=0;z.tasks.claimed={}}z.capacity=Math.max(10,Math.min(200,Math.floor(z.capacity)));z.energy=Math.min(150,z.energy);z.labLevel=Math.max(1,Math.min(10,Math.floor(z.labLevel)));z.prestige=Math.min(999,Math.floor(z.prestige));z.echoDone=x.echoDone===true;z.machines.incubator=Math.max(1,z.machines.incubator);return z}function levelFromResearch(r){let level=1;for(let i=1;i<levelReq.length;i++)if(r>=levelReq[i])level=i+1;return Math.min(10,level)}function updateLevel(){s.labLevel=Math.max(s.labLevel,levelFromResearch(s.research));if(s.meta)s.meta.bestLevel=Math.max(s.meta.bestLevel||1,s.labLevel)}async function save(renderIt=true,cloud=true){s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));if(cloud&&player)try{await player.setData(s,false)}catch{}if(renderIt)render()}function applyElapsed(seconds){const away=Math.min(10800,Math.max(0,num(seconds)));if(!away)return 0;const p=production();let gained=0;for(const k in p)if(p[k]>0){const got=addResource(k,p[k]*away);gained+=got;if(s.tasks)s.tasks.produce=(s.tasks.produce|0)+got}s.energy=Math.min(150,s.energy+away*.08);return gained}async function load(){let local=null;for(const key of [KEY,...OLD_KEYS])try{const x=JSON.parse(localStorage.getItem(key)||'null');if(x&&(!local||num(x.lastSave)>num(local.lastSave)))local=x}catch{}if(player)try{const cloud=await player.getData();if(cloud&&typeof cloud==='object'&&(!local||num(cloud.lastSave)>num(local.lastSave)))local=cloud}catch{}s=normalize(local);const away=Math.min(10800,Math.max(0,(nowMs()-num(s.lastOffline))/1000));const gained=applyElapsed(away);if(away>30&&gained>0)toast('⏱️ Оффлайн (до 3 ч): +'+Math.floor(gained)+' материалов');s.lastOffline=nowMs();updateLevel();render()}function setScreen(id){document.querySelectorAll('.screen').forEach(e=>e.classList.toggle('active',e.id===id));document.querySelectorAll('.nav-item').forEach(e=>e.classList.toggle('active',e.dataset.screen===id));try{render()}catch(err){console.error(err)}if(id==='market'){try{renderMarket()}catch(err){console.error(err)}requestAnimationFrame(()=>{try{renderMarket()}catch(err){console.error(err)}})}if(id==='storyArchive'){try{renderStoryArchive()}catch(err){console.error(err)}}}
function experimentChance(r){let chance=r?50:12;if(s.tech.precision)chance+=10;if(s.tech.adaptation&&selected.some(k=>rare.has(k)))chance+=10;if(!r&&s.tech.analysis)chance+=8;return Math.min(90,chance)}function openComponentPicker(slot){const modal=document.getElementById('componentPicker');if(!modal){toast('Открой склад и выбери материал');try{openLabModal('warehouse')}catch(e){}return}modal.dataset

### fortuneDay
|1,s.labLevel||1);const daily={money:120,research:60,energy:100,capacity:30,labLevel:1,prestige:0,exp:{},hinted:[],discovered:['sample','blood','reagent'],installed:{},echoDone:false,machines:{incubator:1,culture:0,analyzer:0,tissue:0,regenerator:0,synth:0,crystal:0,virusLab:0},tech:clone(base.tech),tasks:clone(base.tasks),adCounts:clone(base.adCounts),adWarehouseUntil:0,adDiscountUntil:0,adProductionUntil:0,orders:{day:'',active:[],claimed:{}},event:{id:'',expires:0},storySeen:{}};Object.assign(s,base,daily);s.meta=meta;s.lastSave=nowMs();s.lastOffline=s.lastSave;selected=[];const modal=document.getElementById('resetConfirmModal');if(modal)modal.classList.add('hidden');await save();toast('💥 Лаборатория уничтожена. Протокол «Чистый лист» выполнен.');try{render()}catch(e){}try{setScreen('home')}catch(e){}}
async function prestige(){if(s.labLevel<5)return toast('Для престижа нужен 5 уровень');if(!confirm('Престиж сбросит материалы, деньги, аппараты и технологии. Ранг останется и даст +5% к производству. Продолжить?'))return;const p=(s.prestige|0)+1;const meta=clone(s.meta||base.meta);meta.resets=(meta.resets||0)+1;meta.bestLevel=Math.max(meta.bestLevel||1,s.labLevel||1);const daily={fortuneDay:s.fortuneDay,fortuneFree:s.fortuneFree,fortuneAds:s.fortuneAds,adDay:s.adDay,adCounts:clone(s.adCounts||base.adCounts),adWarehouseUntil:s.adWarehouseUntil,adDiscountUntil:s.adDiscountUntil,adProductionUntil:s.adProductionUntil,introSeen:s.introSeen,storySeen:clone(s.storySeen||{}),meta:meta};s=clone(base);s.prestige=p;Object.assign(s,daily);s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));selected=[];await save(false,true);render();toast('⭐ Престиж '+p+' — производство +'+(p*5)+'%')}const componentPickerClose=document.getElementById('componentPickerClose');if(componentPickerClose)componentPickerClose.onclick=closeComponentPicker;const componentPicker=document.getElementById('componentPicker');if(componentPicker)componentPicker.addEventListener('click',e=>{if(e.target===componentPicker)closeComponentPicker()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeComponentPicker()});const storyNoteClose=document.getElementById('storyNoteClose');if(storyNoteClose)storyNoteClose.onclick=()=>document.getElementById('storyNote').classList.add('hidden');document.querySelectorAll('[data-screen]').forEach(b=>b.onclick=()=>setScreen(b.dataset.screen));document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('#lab .tab').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('.tab-content').forEach(x=>x.classList.toggle('active',x.id===b.dataset.tab))});document.querySelectorAll('[data-market-tab]').forEach(b=>b.onclick=()=>{marketMode=b.dataset.marketTab==='buy'?'buy':'sell';document.querySelectorAll('[data-market-tab]').forEach(x=>x.classList.toggle('active',x.dataset.marketTab===marketMode));try{renderMarket()}catch(err){console.error(err)}});moneyRes.onclick=()=>setScreen('market');researchRes.onclick=()=>setScreen('researchScreen');energyRes.onclick=()=>{setScreen('home');requestAnimationFrame(()=>{const p=document.getElementById('adBonusPanel');if(p)p.scrollIntoView({behavior:'smooth',block:'center'})})};samplesRes.onclick=()=>setScreen('lab');continueBtn.onclick=()=>setScreen('lab');const storyArchiveBtn=document.getElementById('storyArchiveBtn');if(storyArchiveBtn)storyArchiveBtn.onclick=()=>setScreen('storyArchive');storyArchiveBackBtn.onclick=()=>setScreen('home');const _qf=document.getElementById('questFab');if(_qf)_qf.onclick=()=>openQuestSheet();const _qc=document.getElementById('questSheetClose');if(_qc)_qc.onclick=()=>closeQuestSheet();
const lobbyWheelBtn=document.getElementById('lobbyWheelBtn');if(lobbyWheelBtn)lobbyWheelBtn.onclick=()=>openWheelModal();
const wheelModalClose=document.getElementById('wheelModalClose');if(wheelModalClose)wheelModalClose.onclick=()=>closeWheelModal();
const wheelModal=document.getElementById('wheelModal');if(wheelModal)wheelModal.addEventListener('click',e=>{if(e.target===wheelModal)closeWheelModal()});
document.querySelectorAll('[data-lobby-ad]').forEach(b=>b.onclick=()=>claimAdBonus(b.dataset.l

### fortuneDay
||1);const daily={money:120,research:60,energy:100,capacity:30,labLevel:1,prestige:0,exp:{},hinted:[],discovered:['sample','blood','reagent'],installed:{},echoDone:false,machines:{incubator:1,culture:0,analyzer:0,tissue:0,regenerator:0,synth:0,crystal:0,virusLab:0},tech:clone(base.tech),tasks:clone(base.tasks),adCounts:clone(base.adCounts),adWarehouseUntil:0,adDiscountUntil:0,adProductionUntil:0,orders:{day:'',active:[],claimed:{}},event:{id:'',expires:0},storySeen:{}};Object.assign(s,base,daily);s.meta=meta;s.lastSave=nowMs();s.lastOffline=s.lastSave;selected=[];const modal=document.getElementById('resetConfirmModal');if(modal)modal.classList.add('hidden');await save();toast('💥 Лаборатория уничтожена. Протокол «Чистый лист» выполнен.');try{render()}catch(e){}try{setScreen('home')}catch(e){}}
async function prestige(){if(s.labLevel<5)return toast('Для престижа нужен 5 уровень');if(!confirm('Престиж сбросит материалы, деньги, аппараты и технологии. Ранг останется и даст +5% к производству. Продолжить?'))return;const p=(s.prestige|0)+1;const meta=clone(s.meta||base.meta);meta.resets=(meta.resets||0)+1;meta.bestLevel=Math.max(meta.bestLevel||1,s.labLevel||1);const daily={fortuneDay:s.fortuneDay,fortuneFree:s.fortuneFree,fortuneAds:s.fortuneAds,adDay:s.adDay,adCounts:clone(s.adCounts||base.adCounts),adWarehouseUntil:s.adWarehouseUntil,adDiscountUntil:s.adDiscountUntil,adProductionUntil:s.adProductionUntil,introSeen:s.introSeen,storySeen:clone(s.storySeen||{}),meta:meta};s=clone(base);s.prestige=p;Object.assign(s,daily);s.lastSave=nowMs();s.lastOffline=s.lastSave;localStorage.setItem(KEY,JSON.stringify(s));selected=[];await save(false,true);render();toast('⭐ Престиж '+p+' — производство +'+(p*5)+'%')}const componentPickerClose=document.getElementById('componentPickerClose');if(componentPickerClose)componentPickerClose.onclick=closeComponentPicker;const componentPicker=document.getElementById('componentPicker');if(componentPicker)componentPicker.addEventListener('click',e=>{if(e.target===componentPicker)closeComponentPicker()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeComponentPicker()});const storyNoteClose=document.getElementById('storyNoteClose');if(storyNoteClose)storyNoteClose.onclick=()=>document.getElementById('storyNote').classList.add('hidden');document.querySelectorAll('[data-screen]').forEach(b=>b.onclick=()=>setScreen(b.dataset.screen));document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('#lab .tab').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('.tab-content').forEach(x=>x.classList.toggle('active',x.id===b.dataset.tab))});document.querySelectorAll('[data-market-tab]').forEach(b=>b.onclick=()=>{marketMode=b.dataset.marketTab==='buy'?'buy':'sell';document.querySelectorAll('[data-market-tab]').forEach(x=>x.classList.toggle('active',x.dataset.marketTab===marketMode));try{renderMarket()}catch(err){console.error(err)}});moneyRes.onclick=()=>setScreen('market');researchRes.onclick=()=>setScreen('researchScreen');energyRes.onclick=()=>{setScreen('home');requestAnimationFrame(()=>{const p=document.getElementById('adBonusPanel');if(p)p.scrollIntoView({behavior:'smooth',block:'center'})})};samplesRes.onclick=()=>setScreen('lab');continueBtn.onclick=()=>setScreen('lab');const storyArchiveBtn=document.getElementById('storyArchiveBtn');if(storyArchiveBtn)storyArchiveBtn.onclick=()=>setScreen('storyArchive');storyArchiveBackBtn.onclick=()=>setScreen('home');const _qf=document.getElementById('questFab');if(_qf)_qf.onclick=()=>openQuestSheet();const _qc=document.getElementById('questSheetClose');if(_qc)_qc.onclick=()=>closeQuestSheet();
const lobbyWheelBtn=document.getElementById('lobbyWheelBtn');if(lobbyWheelBtn)lobbyWheelBtn.onclick=()=>openWheelModal();
const wheelModalClose=document.getElementById('wheelModalClose');if(wheelModalClose)wheelModalClose.onclick=()=>closeWheelModal();
const wheelModal=document.getElementById('wheelModal');if(wheelModal)wheelModal.addEventListener('click',e=>{if(e.target===wheelModal)closeWheelModal()});
document.querySelectorAll('[data-lobby-ad]').forEach(b=>b.onclick=()=>claimAdBonus(b.dataset.lobbyAd));

do

### function spinWheel
d="wheelVisual" style="transform:rotate('+wheelAngle+'deg)">🎡</div></div><div class="wheel-result"><div class="wheel-reward" id="wheelReward">'+lastWheelMsg+'</div></div>'+status+'<p style="margin:8px 0 0;font-size:12px;color:#a8c0cc">Нажми на колесо</p>';const body=document.getElementById('wheelModalBody');if(body){body.innerHTML=html;const tap=document.getElementById('wheelTap');if(tap){tap.onclick=()=>onWheelTap();tap.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();onWheelTap()}}}}const legacy=document.getElementById('fortuneHomePanel');if(legacy)legacy.innerHTML='';const badge=document.getElementById('lobbyWheelBtn');if(badge){const t=free>0?(free+'/3'):(ads<10?'🎬':'—');badge.innerHTML='<span class="ico">🎡</span>Колесо'}}
function openWheelModal(){const m=document.getElementById('wheelModal');if(!m)return;renderFortune();m.classList.remove('hidden')}
function closeWheelModal(){const m=document.getElementById('wheelModal');if(m)m.classList.add('hidden')}

function onWheelTap(){if(wheelBusy)return toast('Колесо ещё крутится');if((s.fortuneFree|0)>0)return spinWheel(false);if((s.fortuneAds|0)<10)return spinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магази

### showRewarded
pinWheel(true);toast('Лимит вращений на сегодня исчерпан')}
function spinWheel(ad){if(wheelBusy)return toast('Колесо уже вращается');if(!ad&&(s.fortuneFree|0)<1){if((s.fortuneAds|0)<10)return spinWheel(true);return toast('Бесплатных вращений больше нет')}if(ad&&(s.fortuneAds|0)>=10)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled':'')+'>🎬 Получить</button></div>'}).join('')+'</div>';e.querySelectorAll('[data-ad-bonus]').forEach(b=>b.onclick=()=>claimAdBonus(b.dataset.adBonus))}function claimAdBonus(k){if(adBusy||adActive(k)||s.adCounts[k]>=adLimit(k))return;if(k==='warehouse')return rewarded(()=>{s.adCounts[k]++;s.adWarehouseUntil=Math.max(s.adWarehouseUntil,nowMs())+24*3600*1000;save();toast('📦 +10 мест на складе на 24 часа')});if(k==='discount')return rewarded(()=>{s.adCounts[k]++;s.adDiscountUntil=Math.max(s.adDiscountUntil,nowMs())+30*60*1000;save();toast('🏪 Скидка 25% на 30 минут')});if(k==='production')return rewarded(()=>{s.adCounts[k]++;s.adProductionUntil=Math.max(s.adProductionUntil,nowMs())+30*60*1000;save();toast('🧬 Производство ×2 на 30 минут')});if(k==='money')return rewarded(()=>{s.adCounts[k]+

### showRewarded
)return toast('Лимит рекламных вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled':'')+'>🎬 Получить</button></div>'}).join('')+'</div>';e.querySelectorAll('[data-ad-bonus]').forEach(b=>b.onclick=()=>claimAdBonus(b.dataset.adBonus))}function claimAdBonus(k){if(adBusy||adActive(k)||s.adCounts[k]>=adLimit(k))return;if(k==='warehouse')return rewarded(()=>{s.adCounts[k]++;s.adWarehouseUntil=Math.max(s.adWarehouseUntil,nowMs())+24*3600*1000;save();toast('📦 +10 мест на складе на 24 часа')});if(k==='discount')return rewarded(()=>{s.adCounts[k]++;s.adDiscountUntil=Math.max(s.adDiscountUntil,nowMs())+30*60*1000;save();toast('🏪 Скидка 25% на 30 минут')});if(k==='production')return rewarded(()=>{s.adCounts[k]++;s.adProductionUntil=Math.max(s.adProductionUntil,nowMs())+30*60*1000;save();toast('🧬 Производство ×2 на 30 минут')});if(k==='money')return rewarded(()=>{s.adCounts[k]++;s.money+=200;save();toast('💰 +200 денег')});if(k==='energy')return rewarded(()=>{s.adCounts[k]++;s.energy=150;save();toast('⚡ Энергия полностью восстановлена')});if(k==='research')return rewarded(()=>{s.adCounts[k]++;s.research+=100;updateLevel();save();toast('🔬 +100 науки')})}fu

### onRewarded
 вращений достигнут');if(ad)return rewarded(()=>startWheelSpin(true));startWheelSpin(false)}
function startWheelSpin(ad){if(wheelBusy)return;wheelBusy=true;if(ad)s.fortuneAds++;else s.fortuneFree--;const pool=wheelPool(),k=weightedWheel();let idx=pool.findIndex(x=>x[0]===k);if(idx<0)idx=pool.length-1;const seg=360/pool.length,center=(idx+.5)*seg;const visual=document.getElementById('wheelVisual');if(!visual){wheelBusy=false;return doSpinResult(k,ad)}const turns=5+Math.floor(Math.random()*3);const cur=((wheelAngle%360)+360)%360;const delta=((360-center-cur)%360+360)%360;wheelAngle+=turns*360+delta;visual.style.transition='transform 2.3s cubic-bezier(.12,.75,.12,1)';visual.style.transform='rotate('+wheelAngle+'deg)';setTimeout(()=>{doSpinResult(k,ad);wheelBusy=false;renderFortune()},2350)}async function rewarded(fn){if(adBusy)return toast('Реклама уже запущена');if(!ysdk?.adv?.showRewardedVideo)return toast('Реклама сейчас недоступна');adBusy=true;ysdk.features?.GameplayAPI?.stop?.();let rewardedOnce=false;const reward=()=>{if(rewardedOnce)return;rewardedOnce=true;try{fn()}catch(err){console.error(err);toast('Ошибка выдачи награды')}};try{await ysdk.adv.showRewardedVideo({callbacks:{onRewarded:reward}})}catch{toast('Реклама сейчас недоступна')}finally{adBusy=false;ysdk.features?.GameplayAPI?.start?.()}}let wheelBusy=false,lastWheelMsg="Нажми на колесо";async function doSpinResult(k,ad){if(k==='nothing'){lastWheelMsg='💨 Пустой сектор — в этот раз ничего';toast('🎡 В этот раз — пустой сектор. Повезёт в следующий раз!');save();return}if(addResource(k,1)<1){if(ad)s.fortuneAds--;else s.fortuneFree++;lastWheelMsg='📦 Нет места на складе';return toast('Нет места на складе')}discover(k);lastWheelMsg='🎉 Выигрыш: '+icon(k)+' '+label(k);toast(lastWheelMsg);const e=document.getElementById('wheelReward');if(e){e.textContent=lastWheelMsg;e.classList.add('winner')}save()}const adDefs={energy:['⚡','Полная энергия','Восстановить энергию до 150',3],warehouse:['📦','Расширение склада','+10 ячеек на 24 часа',1],discount:['🏪','Скидка магазина','−25% на покупки на 30 минут',2],production:['🧬','Ускорение производства','×2 производство на 30 минут',2],money:['💰','Денежный грант','+200 денег',2],research:['🔬','Научный грант','+100 науки',3]};function adActive(k){return k==='warehouse'?nowMs()<s.adWarehouseUntil:k==='discount'?nowMs()<s.adDiscountUntil:k==='production'?nowMs()<s.adProductionUntil:false}function adLimit(k){return adDefs[k][3]}function renderAdBonuses(){const e=document.getElementById('adBonusPanel'),show=['energy','research','money','production'];e.innerHTML='<div class=\"notice\">🎬 Реклама используется только для полезных добровольных бонусов. Энергия в рекламе не выдаётся — для полного восстановления есть магазин.</div><div class=\"ad-bonus-grid\">'+show.map(k=>{const d=adDefs[k],active=adActive(k),left=adLimit(k)-s.adCounts[k];return '<div class=\"ad-bonus\"><b>'+d[0]+' '+d[1]+'</b><small>'+d[2]+(active?' · активно':'')+'<br>Сегодня осталось: '+Math.max(0,left)+'</small><button class=\"secondary\" data-ad-bonus=\"'+k+'\" '+(active||left<=0?'disabled':'')+'>🎬 Получить</button></div>'}).join('')+'</div>';e.querySelectorAll('[data-ad-bonus]').forEach(b=>b.onclick=()=>claimAdBonus(b.dataset.adBonus))}function claimAdBonus(k){if(adBusy||adActive(k)||s.adCounts[k]>=adLimit(k))return;if(k==='warehouse')return rewarded(()=>{s.adCounts[k]++;s.adWarehouseUntil=Math.max(s.adWarehouseUntil,nowMs())+24*3600*1000;save();toast('📦 +10 мест на складе на 24 часа')});if(k==='discount')return rewarded(()=>{s.adCounts[k]++;s.adDiscountUntil=Math.max(s.adDiscountUntil,nowMs())+30*60*1000;save();toast('🏪 Скидка 25% на 30 минут')});if(k==='production')return rewarded(()=>{s.adCounts[k]++;s.adProductionUntil=Math.max(s.adProductionUntil,nowMs())+30*60*1000;save();toast('🧬 Производство ×2 на 30 минут')});if(k==='money')return rewarded(()=>{s.adCounts[k]++;s.money+=200;save();toast('💰 +200 денег')});if(k==='energy')return rewarded(()=>{s.adCounts[k]++;s.energy=150;save();toast('⚡ Энергия полностью восстановлена')});if(k==='research')return rewarded(()=>{s.adCounts[k]++;s.research+=100;updateLevel();save();toast('🔬 +100 науки')})}function taskPool(){const t=[];c